NEON_DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require

# Pusher (Step 5)
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=eu
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=eu

# Bridge (default http://localhost:3006)
NEXT_PUBLIC_BRIDGE_URL=http://localhost:3006

# Printers on the local bridge PC
PRINTER_INTERFACE=windows
PRINTER_NAME=EPSON TM-m30 Receipt
PRINTER_CHARACTER_SET=PC858_EURO
KITCHEN_PRINTER_HOST=192.168.1.143
KITCHEN_PRINTER_PORT=9100
